# Frontend environment variables
# Copy to .env.local for local development, or .env.production for production build

# In development (npm run dev): leave empty — Vite proxy handles /api/ → localhost:5000
VITE_API_BASE_URL=

# In production: set to your backend URL (no trailing slash)
# VITE_API_BASE_URL=https://api.krishividya.com
